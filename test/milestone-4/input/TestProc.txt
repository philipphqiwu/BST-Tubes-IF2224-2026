program TestProc;

var
  i, total: integer;

procedure Add(x: integer);
begin
  total := total + x;
end;

begin
  total := 0;
  for i := 1 to 10 do
    Add(i);
  writeln('sum 1..10 = ', total);

  total := 100;
  for i := 5 downto 1 do
    Add(-i);
  writeln('after subtraction = ', total);
end.
